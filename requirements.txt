# --- Computer Vision & ML ---
ultralytics>=8.0.0       # For YOLO object detection and tracking
torch>=1.9.0             # Required by ultralytics and for general ML
torchvision>=0.10.0        # For vision-specific PyTorch utilities
opencv-python>=4.5.0     # For video and image processing (cv2)
Pillow>=9.0.0            # For image manipulation (PIL)
numpy>=1.20.0            # For numerical operations
scipy>=1.7.0             # For scientific computing, e.g., cosine distance
face-recognition>=1.3.0  # For face detection and recognition (uses dlib)

# --- Azure & OpenAI SDKs ---
openai>=1.3.0            # For interacting with OpenAI and Azure OpenAI models
azure-identity>=1.12.0   # For Azure authentication
azure-storage-blob>=12.9.0 # For Azure Blob Storage interaction
tiktoken>=0.3.0          # For OpenAI token counting

# --- Video Processing Tools ---
PySceneDetect[opencv]>=0.6.2 # For scene detection (opencv extra for performance)

# --- Utilities ---
python-dotenv>=0.19.0    # For loading environment variables from .env files
pydantic>=1.9.0,<2.0.0   # For data validation and settings management (using v1 features)
nest-asyncio>=1.5.0      # To allow asyncio event loops to be nested

# Add other specific dependencies or versions as needed below 